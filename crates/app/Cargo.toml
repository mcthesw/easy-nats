[package]
name = "easy-nats"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Desktop GUI client for NATS servers, JetStream, KV, and Object Store"
homepage = "https://github.com/mcthesw/easy-nats"
repository.workspace = true
authors = ["mcthesw"]
readme = "../../README.md"

[[bin]]
name = "easy-nats"
path = "src/main.rs"

[dependencies]
nats-backend = { path = "../nats-backend" }
base64 = "0.22"
# Keep native desktop functionality, but avoid eframe's default feature bundle
# (notably wgpu + web_screen_reader) since this app explicitly uses Glow.
eframe = { version = "0.34", default-features = false, features = ["glow", "default_fonts", "accesskit"] }
egui_dock = "0.19.1"
egui_plot = "0.35.0"
mimalloc = "0.1"
protox = "0.9"
prost = "0.14"
prost-reflect = { version = "0.16", features = ["serde"] }
prost-types = "0.14"
rfd = "0.17"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9.34"
tracing = "0.1"
tracing-appender = "0.2"
tracing-subscriber = { version = "0.3", features = ["env-filter", "registry", "fmt"] }
tokio-util = "0.7"
image = { version = "0.25", default-features = false, features = ["png"] }

[target.'cfg(target_os = "linux")'.dependencies]
# Linux desktop support still needs both backends enabled.
eframe = { version = "0.34", default-features = false, features = ["x11", "wayland"] }

[target.'cfg(target_os = "windows")'.build-dependencies]
winresource = "0.1"

# Package metadata for cargo-packager.
[package.metadata.packager]
product-name = "Easy NATS"
identifier = "io.github.mcthesw.easy-nats"
category = "DeveloperTool"
icons = ["../../packaging/icon.png"]
long-description = """
Easy NATS is a cross-platform desktop client for NATS. It provides a tabbed UI \
for managing connections, publishing and subscribing to subjects, browsing \
JetStream streams and consumers, and inspecting KV and Object Store buckets.
"""

[package.metadata.packager.deb]
section = "net"
priority = "optional"
depends = [
    "libc6",
    "libgcc-s1",
    "libxkbcommon0",
    "libxcb1",
    "libxcb-render0",
    "libxcb-shape0",
    "libxcb-xfixes0",
    "libgl1",
    "libwayland-client0",
    "libssl3 | libssl1.1",
]

[package.metadata.packager.deb.files]
"../../packaging/easy-nats.desktop" = "/usr/share/applications/easy-nats.desktop"

[package.metadata.packager.appimage]
[package.metadata.packager.appimage.files]
"../../packaging/easy-nats.desktop" = "usr/share/applications/easy-nats.desktop"

[package.metadata.packager.macos]
minimum-system-version = "10.15"

# RPM metadata for cargo-generate-rpm.
[package.metadata.generate-rpm]
assets = [
    { source = "target/release/easy-nats", dest = "/usr/bin/easy-nats", mode = "755" },
    { source = "packaging/easy-nats.desktop", dest = "/usr/share/applications/easy-nats.desktop", mode = "644" },
    { source = "packaging/icon.png", dest = "/usr/share/icons/hicolor/512x512/apps/easy-nats.png", mode = "644" },
    { source = "LICENSE", dest = "/usr/share/licenses/easy-nats/LICENSE", mode = "644" },
]

