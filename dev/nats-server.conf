# NATS Server config for local development/testing
listen: 0.0.0.0:4222
http: 0.0.0.0:8222

jetstream {
  store_dir: /data
  max_mem: 256MB
  max_file: 1GB
}

# Simple token auth for testing auth features
authorization {
  token: "dev-secret-token"
}
